# CRUD Node.js + Oracle Database

Este é um projeto simples de API REST para realizar operações de CRUD (Create, Read, Update, Delete) em uma tabela de usuários dentro de um banco de dados Oracle.

## 🚀 Tecnologias Utilizadas

* **Node.js**
* **Express** (Framework Web)
* **Oracle Database** (Banco de dados)
* **Sequelize** (ORM para Node.js)  
* **Oracle SQL Developer** (Interface para gerenciar o banco de dados)
* **Postman** (Interface para testar as rotas)
* **Git** (Controle de versão)
* **GitHub** (Repositório remoto)
* **node-oracledb** (Driver oficial da Oracle para Node.js)
* **dotenv** (Gerenciamento de variáveis de ambiente),

## 📁 Estrutura de Pastas

```text
node-oracle-crud/
├── src/
│   ├── config/       # Conexão com o banco
│   ├── controllers/  # Lógica das rotas
│   └── app.js        # Arquivo principal
├── .env              # Credenciais (não enviar para o Git)
├── package.json      # Dependências
└── README.md         # Documentação